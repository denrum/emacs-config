;;; package -- Summary
;;; 05-which-key.el -*- lexical-binding: t; -*-
;;; Commentary:

;;; Code:

(use-package which-key
  :ensure t
  :config
  (which-key-mode)
  (which-key-setup-minibuffer))

(provide '05-which-key)

;;; 05-which-key.el ends here
